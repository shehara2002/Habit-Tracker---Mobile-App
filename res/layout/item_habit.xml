<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="60dp"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:paddingVertical="8dp"
    android:gravity="center_vertical"
    android:background="@drawable/habit_item_bg"
    android:clickable="true"
    android:focusable="true"
    android:layout_marginBottom="8dp"
    android:elevation="1dp">      <!-- ✅ Adds a soft shadow -->><!-- Side Line (changes color dynamically) -->
    <View
        android:id="@+id/sideLine"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="8dp"
        android:background="@drawable/rounded_purple_line" />

    <!-- Habit Text -->
    <TextView
        android:id="@+id/habitText"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:text="Morning Walk"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Tick Button (mark as completed) -->
    <ImageView
        android:id="@+id/tickButton"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_check"
        app:tint="@color/primary"
        android:layout_marginEnd="12dp"
        android:contentDescription="Mark as Completed"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="6dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- Edit Icon -->
    <ImageView
        android:id="@+id/editButton"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:src="@drawable/ic_edit"
        app:tint="@color/primary"
        android:layout_marginEnd="12dp"
        android:contentDescription="Edit Habit"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="4dp" />

    <!-- Delete Icon -->
    <ImageView
        android:id="@+id/deleteButton"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:src="@drawable/ic_delete"
        app:tint="@color/primary"
        android:contentDescription="Delete Habit"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="4dp" />

</LinearLayout>
